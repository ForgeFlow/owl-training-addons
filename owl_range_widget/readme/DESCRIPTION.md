Widget creation using OWL. This module creates a new Range widget that
is later used in the Form view of the res.partner model, to display a
new field which represents the expected salary of a partner.
